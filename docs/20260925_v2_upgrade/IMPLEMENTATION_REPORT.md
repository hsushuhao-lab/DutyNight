# M7 Guard Post Interaction Repair

## Status

Local implementation and browser QA passed. GitHub Pages deployment and public browser QA remain pending until this release is pushed and the exact-SHA workflows finish.

## Fixed release source

- Implementation commit: `f6c8b013f01eca3f0d59ca8dd2566b1b9e0b1e98`
- Issue: `M7 GUARD POST INTERACTION BROKEN` (P0)
- Scope: `FirstCampus1F`, M7 interaction and objective text, story browser QA, and the durable QA contract.

## Player-visible behavior

1. The player enters 1F at the main-lobby spawn and physically walks to the old guard post.
2. Aiming at its desk presents `[E] 檢查舊警衛台`.
3. After inspection, the concealed panel seams, old lock plate, keyhole, and purple indicator appear; the objective changes to `檢查警衛台後方浮現的舊門框`.
4. The revealed door hitbox becomes interactable in the live scene. The player walks around the counter, aims at the visible frame, and receives its `[E]` prompt.
5. Pressing E opens the existing 02:17 B-Panel choice. The 1F scene rebuilds the revealed state from the persistent story flag when the player returns.

The controller interaction distance is unchanged. The QA bridge only sets the player's view direction; movement, raycast target selection, prompts, and E key presses are exercised through the browser.

## Local verification

At implementation SHA `f6c8b013f01eca3f0d59ca8dd2566b1b9e0b1e98`:

- JavaScript suite: 154 checks passed, 0 failed.
- Production build: passed; Vite transformed 67 modules.
- Full M2–M9 browser playthrough: `PASS`, 10 milestones, 26 story screenshots, 6 motion screenshots, 2 M7 functional screenshots, 1 physical M7 interaction flow, 0 screenshot warnings, 0 errors.
- M7 browser flow began at the 1F main-lobby spawn, walked to the desk, received and used the desk E prompt, revealed the panel, walked to the panel, received and used its E prompt, and reached the B-Panel choice.
- Evidence directory (local, generated by the browser run): `prototype/qa-results/v2-2_0-guard-post-p0-release-f6c8b013/`.
- Review artifact source is the exact implementation SHA above. Public release still requires the `Fast Deploy GitHub Pages` and `Story Browser Playthrough` workflows to pass for the pushed master SHA.

The build emits the existing Vite warning for a minified JavaScript chunk above 1,000 kB (1,149.08 kB). The build exits successfully; chunk splitting is outside this repair.

## Durable QA contract

`QA_V2.md` now requires the physical M7 guard-post-to-B-Panel walkthrough and both functional screenshots. Do not replace that gate with direct handler invocation or a QA bridge interaction shortcut. The historical `ACT1_MASTER_SPEC` scope must not be used to remove current gameplay content.
