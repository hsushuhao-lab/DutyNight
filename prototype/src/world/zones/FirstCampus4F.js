import { WardFloorplan } from '../shared/WardFloorplan.js';

export class FirstCampus4F extends WardFloorplan {
  constructor(scene,geometryFactory){super(scene,geometryFactory,{campus:'first',floor:4});}
}
