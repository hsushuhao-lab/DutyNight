// The Sep 20 floorplan supersedes the old 18 hard-coded routes.
// Replacement exercises the production controller, every new room, both states of gates,
// all shared vertical cores, portal boundaries and 20 full cleanup cycles.
import './scripts/test-floorplan-qa.mjs';
